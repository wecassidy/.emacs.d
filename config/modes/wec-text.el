;;; wec-text.el --- text-mode customizations
;;; Commentary:
;;; Code:

(add-hook 'text-mode-hook 'turn-on-flyspell)
(add-hook 'text-mode-hook 'turn-on-auto-fill)

(provide 'wec-text)
;;; wec-text.el ends here
